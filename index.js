//git add . (to add all the files that are untracked for tracking)
let name ="Anamika";
let surname="Singh";
let age=22;

function isAbove()
{
    if(age>=18)
    {
        return true;
    }
}
function cacl(a,b)
{
    return a+b;
}
