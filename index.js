//git add . (to add all the files that are untracked for tracking)
// git blame [filename] to find the lines of code written by whom
// git show f81106f(hashvalue/SHA value) to see the changes done in that version/commit.
//git log --oneline (to shorten the hash value)
//git reset --hard 9893196 (to rollback to the previous version)
let name ="Anamika";
let surname="Singh";
let age=22;

function isAbove()
{
    if(age>=18)
    {
        return true;
    }
}
function cacl(a,b)
{
    return a+b;
}

function diff(a,b)
{
    return a-b;
}
function pro(a,b)
{
    return a*b;
}